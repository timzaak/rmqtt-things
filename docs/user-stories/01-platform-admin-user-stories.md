# Platform Admin 用户故事

> 角色定义以 `docs/user-stories/_roles.md` 为准。

---

## 1. 产品管理

### 故事 1：创建产品 [US-PA-001]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在后台创建新的 IoT 产品，填写名称、型号和描述
**从而**：将同类设备归入同一产品进行统一管理

**【验收标准】**

**场景 1：正常创建产品**
```gherkin
Given 管理员已登录后台
When 在产品列表页点击"Create Product"，填写产品名称、型号编号并提交
Then 系统创建产品，页面跳转到产品列表，新产品出现在列表中
```

**场景 2：型号编号重复**
```gherkin
Given 系统中已存在型号编号为 "SENSOR-A01" 的产品
When 管理员创建新产品时使用相同型号编号
Then 系统提示创建失败，产品未被创建
```

---

### 故事 2：查看产品列表 [US-PA-002]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在后台查看所有产品的列表，并支持按名称或型号搜索
**从而**：快速定位到需要管理的产品

**【验收标准】**

**场景 1：查看产品列表**
```gherkin
Given 系统中已有多个产品
When 管理员进入产品列表页
Then 系统展示所有产品的 ID、名称、型号编号、描述、状态、创建时间和更新时间
```

**场景 2：搜索产品**
```gherkin
Given 系统中已有多个产品
When 管理员在搜索框输入关键词并提交
Then 系统展示名称或型号编号匹配关键词的产品
```

---

### 故事 3：编辑产品 [US-PA-003]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：修改已有产品的名称和描述
**从而**：更正产品信息或补充说明

**【验收标准】**

**场景 1：正常编辑产品**
```gherkin
Given 管理员已登录后台，且存在至少一个产品
When 管理员在产品列表点击"Edit"，修改名称和描述后提交
Then 系统保存修改，页面跳转到产品列表，修改后的信息已更新
```

**场景 2：型号编号不可修改**
```gherkin
Given 管理员正在编辑某个产品
When 页面加载完成
Then 型号编号字段为只读状态，无法编辑
```

---

## 2. 证书管理

### 故事 4：签发设备证书 [US-PA-004]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：为指定产品的设备签发 TLS 证书，设定有效期
**从而**：设备可使用证书进行 MQTT 安全连接

**【验收标准】**

**场景 1：正常签发证书**
```gherkin
Given 管理员已登录后台，且存在目标产品
When 管理员选择产品、输入设备 ID、设定起止时间并提交
Then 系统签发证书，返回证书内容，页面跳转到证书列表
```

**场景 2：设备已有有效证书**
```gherkin
Given 指定设备已存在状态为 Normal 且未过期的证书
When 管理员尝试为该设备再次签发证书（未勾选强制重签）
Then 系统提示"证书已存在且有效"，拒绝签发
```

**场景 3：强制重签证书**
```gherkin
Given 指定设备已存在有效证书
When 管理员勾选"Force re-issue"并提交
Then 系统重新签发证书，新证书替代旧证书
```

---

### 故事 5：查看证书列表 [US-PA-005]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在后台查看所有已签发证书的列表，按产品和设备 ID 筛选
**从而**：了解证书签发状况和到期情况

**【验收标准】**

**场景 1：查看证书列表**
```gherkin
Given 系统中已签发多个证书
When 管理员进入证书列表页
Then 系统展示所有证书的 ID、产品、设备、创建时间、到期时间和状态
```

**场景 2：按产品和设备筛选**
```gherkin
Given 系统中已签发多个证书
When 管理员选择产品和/或输入设备 ID 进行搜索
Then 系统仅展示匹配条件的证书
```

---

### 故事 6：吊销/作废证书 [US-PA-006]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：吊销或作废某个设备的证书
**从而**：阻止该设备使用旧证书继续连接

**【验收标准】**

**场景 1：吊销证书**
```gherkin
Given 存在一个状态为 Normal 的证书
When 管理员点击"Revoke"并确认操作
Then 证书状态变更为 Revoked，证书列表中状态已更新
```

**场景 2：作废证书**
```gherkin
Given 存在一个状态为 Normal 的证书
When 管理员点击"Invalidate"并确认操作
Then 证书状态变更为 Invalid，证书列表中状态已更新
```

**场景 3：非 Normal 状态证书不可操作**
```gherkin
Given 存在一个状态为 Revoked 或 Invalid 的证书
When 证书列表渲染该行
Then 该行不显示 Revoke 和 Invalidate 操作按钮
```

---

### 故事 23：查看证书详情 [US-PA-023]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：在证书列表中点击某条证书查看其完整详情
**从而**：确认证书的具体信息和状态

**【验收标准】**

**场景 1：查看证书详情**
```gherkin
Given 存在一条证书记录
When 管理员在证书列表点击该证书的查看操作
Then 页面展示证书的完整信息，包括 ID、产品、设备 ID、证书内容（PEM 格式）、起止时间、状态和创建时间
```

**场景 2：返回证书列表**
```gherkin
Given 管理员正在查看证书详情页
When 点击返回链接
Then 页面跳转回证书列表页
```

---

### 故事 24：下载已签发证书和私钥 [US-PA-024]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：证书签发成功后能下载证书和私钥文件
**从而**：将证书和密钥部署到目标设备

**【验收标准】**

**场景 1：签发成功后展示下载**
```gherkin
Given 管理员在签发证书页面提交了签发请求
When 证书签发成功
Then 页面展示证书内容和私钥内容，并提供下载按钮
```

**场景 2：下载证书文件**
```gherkin
Given 证书签发成功且页面展示了下载区域
When 管理员点击下载证书按钮
Then 浏览器下载证书 PEM 文件
```

**场景 3：下载私钥文件**
```gherkin
Given 证书签发成功且页面展示了下载区域
When 管理员点击下载私钥按钮
Then 浏览器下载私钥 PEM 文件
```

---

### 故事 25：下载 CA 证书 [US-PA-025]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：从管理后台下载平台的 CA 根证书
**从而**：在设备端或 MQTT Broker 配置中信任该 CA

**【验收标准】**

**场景 1：下载 CA 证书**
```gherkin
Given 管理员已登录后台
When 管理员在证书管理区域点击下载 CA 证书
Then 浏览器下载 CA 证书 PEM 文件
```

---

## 3. 事件校验模板管理

### 故事 7：创建校验模板 [US-PA-007]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：为产品创建事件校验模板，定义事件名称和 JSON Schema
**从而**：平台在接收设备属性上报时可以按 Schema 校验数据

**【验收标准】**

**场景 1：正常创建模板**
```gherkin
Given 管理员已登录后台
When 管理员选择产品、填写事件名称、描述和 JSON Schema 后提交
Then 系统创建模板（状态为 Draft），页面跳转到模板列表
```

**场景 2：Schema 格式无效**
```gherkin
Given 管理员正在创建校验模板
When 提交的 JSON Schema 格式不符合 JSON Schema 规范
Then 系统提示"Invalid JSON schema"，模板未创建
```

---

### 故事 8：查看校验模板列表 [US-PA-008]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在后台查看所有校验模板列表，按产品和事件名称筛选
**从而**：了解各产品的校验模板配置情况

**【验收标准】**

**场景 1：查看模板列表**
```gherkin
Given 系统中已有多个校验模板
When 管理员进入模板列表页
Then 系统展示所有模板的 ID、产品 ID、事件名称、描述、状态、创建时间和更新时间
```

**场景 2：按产品和事件筛选**
```gherkin
Given 系统中已有多个校验模板
When 管理员选择产品和/或输入事件名称进行搜索
Then 系统仅展示匹配条件的模板
```

---

### 故事 9：查看校验模板详情 [US-PA-009]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看某个校验模板的完整详情，包括 Schema 内容
**从而**：确认模板配置是否正确

**【验收标准】**

**场景 1：查看模板详情**
```gherkin
Given 存在一个校验模板
When 管理员在模板列表点击"View"
Then 系统展示模板的 ID、产品 ID、事件名称、描述、状态、Schema 内容、创建时间和更新时间，并提供"Edit"入口
```

---

### 故事 10：编辑校验模板 [US-PA-010]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：修改校验模板的描述、Schema 和状态
**从而**：更新模板配置或激活/停用模板

**【验收标准】**

**场景 1：正常编辑模板**
```gherkin
Given 存在一个 Draft 或 Inactive 状态的校验模板
When 管理员修改描述和/或 Schema 后提交
Then 系统保存修改，页面跳转到模板列表
```

**场景 2：Active 状态模板的 Schema 不可修改**
```gherkin
Given 存在一个 Active 状态的校验模板
When 管理员进入编辑页面
Then Schema 编辑器为只读状态，无法修改
```

**场景 3：更新模板状态**
```gherkin
Given 管理员正在编辑模板
When 管理员将状态从 Draft 改为 Active 并提交
Then 系统更新模板状态，属性校验缓存被清除以使用新 Schema
```

---

## 4. OTA 固件管理

### 故事 11：创建 OTA 版本 [US-PA-011]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：在创建页面填写版本信息并上传固件文件，完成 OTA 版本创建
**从而**：准备向设备推送固件升级

**【验收标准】**

**场景 1：正常创建 OTA 版本**
```gherkin
Given 管理员已登录后台
When 管理员在创建页面选择产品、填写版本 key、版本号（x.y.z 格式）、最小版本、上传固件文件后提交
Then 系统创建 OTA 版本记录，页面跳转到 OTA 版本列表
```

**场景 2：上传固件文件**
```gherkin
Given 管理员正在创建 OTA 版本
When 管理员选择固件文件并触发上传
Then 系统自动计算文件大小和 MD5 哈希值，将文件上传至对象存储，并在表单中展示 file_key、bin_length 和 bin_md5
```

**场景 3：版本格式校验**
```gherkin
Given 管理员正在填写版本号字段
When 输入的版本号不符合 x.y.z 格式（如 "abc" 或 "1.2"）
Then 页面提示格式错误
```

**场景 4：指定设备范围**
```gherkin
Given 管理员正在创建 OTA 版本
When 管理员逐个添加目标设备 ID 并提交
Then 该固件版本仅对列表中的设备生效
```

**场景 5：必填字段缺失**
```gherkin
Given 管理员正在创建 OTA 版本
When 未填写产品、版本 key、版本号、最小版本或未上传固件文件就提交
Then 页面提示缺少必填字段，不发送创建请求
```

**场景 6：未保存离开提示**
```gherkin
Given 管理员已在创建页面填写了部分信息
When 管理员尝试离开当前页面
Then 页面弹出确认提示，防止意外丢失填写内容
```

---

### 故事 12：查看 OTA 版本列表 [US-PA-012]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：在后台查看所有 OTA 固件版本列表，按产品筛选，查看版本格式化信息
**从而**：了解固件发布情况

**【验收标准】**

**场景 1：查看 OTA 版本列表**
```gherkin
Given 系统中已有多个 OTA 版本
When 管理员进入 OTA 版本列表页
Then 系统展示版本列表，包含 ID、产品名称、Key、版本号、最小版本、最大版本、文件大小、MD5、创建时间，支持按产品筛选和分页浏览
```

**场景 2：版本号格式化展示**
```gherkin
Given 系统中存储的版本号为整数格式
When 管理员查看 OTA 版本列表或详情
Then 版本号以 x.y.z 格式展示（如 100000 展示为 1.0.0）
```

**场景 3：删除确认**
```gherkin
Given 管理员在版本列表点击"Delete"
Then 页面弹出确认对话框，展示待删除版本的 key 和版本号
When 管理员确认删除
Then 系统移除该版本记录，列表刷新
```

---

### 故事 13：编辑/删除 OTA 版本 [US-PA-013]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：修改或删除 OTA 固件版本记录
**从而**：更正配置信息或取消已发布的版本

**【验收标准】**

**场景 1：更新 OTA 版本**
```gherkin
Given 存在一个 OTA 版本记录
When 管理员修改最小版本、最大版本、固件文件、更新日志或设备范围后提交
Then 系统保存修改，页面跳转到版本列表
```

**场景 2：编辑页只读字段**
```gherkin
Given 管理员正在编辑某个 OTA 版本
Then 产品、版本 key 和版本号字段为只读状态，不可修改
```

**场景 3：重新上传固件文件**
```gherkin
Given 管理员正在编辑 OTA 版本
When 管理员选择新的固件文件上传
Then 系统重新计算文件大小和 MD5，更新 file_key、bin_length 和 bin_md5
```

**场景 4：删除 OTA 版本**
```gherkin
Given 存在一个 OTA 版本记录
When 管理员删除该版本
Then 系统移除该版本记录（软删除），版本不再出现在列表中
```

**场景 5：编辑页未保存离开提示**
```gherkin
Given 管理员已在编辑页面修改了部分信息
When 管理员尝试离开当前页面
Then 页面弹出确认提示，防止意外丢失修改
```

---

### 故事 21：查看 OTA 版本详情 [US-PA-021]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看某个 OTA 固件版本的完整详情
**从而**：确认版本配置和固件文件信息

**【验收标准】**

**场景 1：查看版本详情**
```gherkin
Given 存在一个 OTA 版本记录
When 管理员在版本列表点击"Show"
Then 页面展示版本的完整信息，包括 ID、产品名称、Key、版本号、最小版本、最大版本、File Key、更新日志、文件大小、MD5、发布时间、状态、创建时间，以及目标设备 ID 列表
```

**场景 2：返回版本列表**
```gherkin
Given 管理员正在查看版本详情页
When 点击"Back to List"链接
Then 页面跳转到 OTA 版本列表
```

---

## 5. 设备管理

### 故事 14：查看设备状态列表 [US-PA-014]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看所有设备的在线/离线状态，按产品和状态筛选
**从而**：监控设备连接状况

**【验收标准】**

**场景 1：查看设备状态**
```gherkin
Given 系统中已有设备连接记录
When 管理员查看设备状态列表
Then 系统展示设备的在线/离线状态、IP 地址、最后在线/离线时间等信息
```

**场景 2：按状态筛选**
```gherkin
Given 系统中已有多个设备
When 管理员筛选在线/离线状态
Then 系统仅展示匹配状态的设备
```

---

### 故事 15：查看设备属性历史 [US-PA-015]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看设备的属性上报历史和最新属性
**从而**：了解设备运行数据和状态变化

**【验收标准】**

**场景 1：查看设备最新属性**
```gherkin
Given 某设备已上报属性
When 管理员查询该设备的最新属性
Then 系统返回该设备最近一次上报的属性数据
```

**场景 2：查看属性历史**
```gherkin
Given 某设备已多次上报属性
When 管理员查询该设备的属性历史
Then 系统返回按时间倒序排列的属性上报记录
```

---

### 故事 16：下发属性命令 [US-PA-016]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：向指定设备下发属性设置命令
**从而**：远程控制设备行为

**【验收标准】**

**场景 1：设备在线时下发命令**
```gherkin
Given 目标设备已订阅属性设置主题
When 管理员提交属性命令
Then 系统创建命令记录并立即推送到设备，命令状态变为 Sent
```

**场景 2：设备离线时下发命令**
```gherkin
Given 目标设备未订阅属性设置主题
When 管理员提交属性命令
Then 系统创建命令记录（状态为 Pending），待设备上线后推送
```

**场景 3：删除待处理命令**
```gherkin
Given 存在状态为 Pending 的命令
When 管理员选择并删除这些命令
Then 系统移除命令记录
```

---

### 故事 17：查看设备事件历史 [US-PA-017]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看设备的事件上报历史
**从而**：了解设备运行中产生的事件

**【验收标准】**

**场景 1：查看事件历史**
```gherkin
Given 某设备已上报事件
When 管理员查询该设备的事件历史
Then 系统返回按时间倒序排列的事件记录
```

---

### 故事 18：查看设备状态变更历史 [US-PA-018]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看设备的连接/断开历史记录
**从而**：追踪设备连接状态变化

**【验收标准】**

**场景 1：查看状态变更历史**
```gherkin
Given 某设备有多次连接/断开记录
When 管理员查询该设备的状态变更历史
Then 系统返回包含连接时间、断开时间、IP 地址和原因的历史记录
```

---

### 故事 19：设备列表页面 [US-PA-019]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在 Web 后台通过设备列表页面查看所有设备，并按产品和在线/离线状态筛选，分页浏览
**从而**：快速定位和监控目标设备的连接状况

**【验收标准】**

**场景 1：查看设备列表**
```gherkin
Given 系统中已有设备连接记录
When 管理员进入设备列表页面
Then 系统展示所有设备的 device_id、product_id、在线/离线状态、IP 地址、最后在线时间等信息，并支持分页浏览
```

**场景 2：按产品筛选设备**
```gherkin
Given 系统中存在多个产品的设备
When 管理员在设备列表页选择某个产品进行筛选
Then 系统仅展示属于该产品的设备
```

**场景 3：按在线/离线状态筛选**
```gherkin
Given 系统中已有多个设备
When 管理员在设备列表页选择在线或离线状态进行筛选
Then 系统仅展示匹配状态的设备
```

**场景 4：点击设备进入详情**
```gherkin
Given 设备列表页已展示设备数据
When 管理员点击某个设备的 device_id
Then 页面跳转到该设备的详情页面
```

---

### 故事 20：设备详情页面 [US-PA-020]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：在设备详情页面查看设备的完整信息，包括最新属性、属性上报历史、事件历史、命令历史和连接状态历史
**从而**：全面了解单个设备的运行状态和历史行为

**【验收标准】**

**场景 1：查看设备详情基本信息**
```gherkin
Given 存在一个已接入的设备
When 管理员进入该设备的详情页面
Then 页面展示设备的基本信息，包括 device_id、product_id、当前在线/离线状态、IP 地址和最后在线/离线时间
```

**场景 2：查看设备最新属性**
```gherkin
Given 某设备已上报属性
When 管理员在设备详情页查看最新属性区域
Then 页面展示该设备最近一次上报的所有属性数据
```

**场景 3：查看属性上报历史**
```gherkin
Given 某设备已多次上报属性
When 管理员在设备详情页查看属性历史区域
Then 页面展示按时间倒序排列的属性上报记录，并支持分页浏览
```

**场景 4：查看事件上报历史**
```gherkin
Given 某设备已上报事件
When 管理员在设备详情页查看事件历史区域
Then 页面展示按时间倒序排列的事件记录，并支持分页浏览
```

**场景 5：查看命令历史**
```gherkin
Given 管理员曾向某设备下发过属性命令
When 管理员在设备详情页查看命令历史区域
Then 页面展示该设备的所有属性命令记录，包含命令内容、状态和创建时间，并支持分页浏览
```

**场景 6：查看连接状态变更历史**
```gherkin
Given 某设备有多次连接/断开记录
When 管理员在设备详情页查看状态历史区域
Then 页面展示该设备的连接/断开历史记录，包含连接时间、断开时间、IP 地址和原因
```

---

## 6. 文件上传

### 故事 22：获取文件上传凭证 [US-PA-022]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：通过管理后台获取文件上传凭证（S3 预签名 URL）
**从而**：将文件（如 OTA 固件）直接上传到对象存储

**【验收标准】**

**场景 1：正常获取上传凭证**
```gherkin
Given 管理员已登录后台，平台已配置 S3 存储
When 管理员在创建/编辑 OTA 版本页面选择固件文件并触发上传
Then 系统返回 S3 预签名上传地址和字段，前端使用该凭证将文件直传 S3
```

**场景 2：目录不被允许**
```gherkin
Given 管理员请求上传到不在白名单中的目录
When 提交文件上传请求
Then 系统拒绝请求，提示"Directory not allowed"
```

---

## 7. 认证与权限

### 故事 26：管理员登录管理后台 [US-PA-026]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：通过统一认证服务登录后才能访问管理后台
**从而**：确保只有经过授权的人员才能操作系统管理功能

**【验收标准】**

**场景 1：正常登录**
```gherkin
Given 管理员已拥有统一认证服务的账号
When 管理员访问管理后台
Then 系统将管理员重定向到统一登录页面，登录成功后返回管理后台首页
```

**场景 2：未登录直接访问**
```gherkin
Given 管理员未登录
When 直接访问管理后台的某个页面
Then 系统重定向到统一登录页面，登录成功后跳转回原访问页面
```

**场景 3：设备端接口不受影响**
```gherkin
Given 设备使用 HMAC 认证与平台通信
When 管理员登录体系发生变更
Then 设备端的 HMAC 认证方式保持不变，设备通信正常
```

---

### 故事 27：管理员权限访问控制 [US-PA-027]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：只能访问我有权限的管理功能
**从而**：确保不同角色的管理员只能操作其职责范围内的功能

**【验收标准】**

**场景 1：有权限的操作**
```gherkin
Given 管理员已登录且拥有某项功能的操作权限
When 管理员访问该功能
Then 系统正常响应，完成操作
```

**场景 2：无权限的操作**
```gherkin
Given 管理员已登录但不拥有某项功能的操作权限
When 管理员尝试访问该功能
Then 系统拒绝访问，提示无操作权限
```

**场景 3：无登录凭据访问**
```gherkin
Given 请求未携带有效的登录凭据
When 访问任何管理功能
Then 系统拒绝访问，提示未登录
```

---

### 故事 28：会话过期处理 [US-PA-028]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：当登录会话过期时，系统自动引导我重新登录
**从而**：避免在会话失效后遇到不明确的错误提示

**【验收标准】**

**场景 1：操作中会话过期**
```gherkin
Given 管理员已登录但会话已过期
When 管理员在管理后台进行任何操作
Then 系统自动重定向到统一登录页面，重新登录后跳转回当前页面
```

**场景 2：页面加载时会话已过期**
```gherkin
Given 管理员已登录但会话已过期
When 刷新或打开管理后台页面
Then 系统自动重定向到统一登录页面
```

---

## 8. 告警规则管理

### 故事 29：创建告警规则 [US-PA-029]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：为指定产品创建告警规则，定义触发条件、判断逻辑和执行动作
**从而**：当设备上报数据满足条件时自动触发告警

**【验收标准】**

**场景 1：创建属性阈值规则**
```gherkin
Given 管理员已登录后台，且存在目标产品
When 管理员选择产品，设置规则名称，选择触发类型为"属性"，指定属性名和条件（如 temperature > 50），配置告警动作和级别后提交
Then 系统创建规则，页面跳转到规则列表，新规则出现在列表中
```

**场景 2：创建事件触发规则**
```gherkin
Given 管理员已登录后台，且存在目标产品
When 管理员选择产品，设置规则名称，选择触发类型为"事件"，指定事件标识后提交
Then 系统创建规则，当设备上报匹配的事件时规则将被触发
```

**场景 3：创建设备状态规则**
```gherkin
Given 管理员已登录后台，且存在目标产品
When 管理员选择产品，设置规则名称，选择触发类型为"设备上线"或"设备离线"后提交
Then 系统创建规则，当该产品下设备发生对应状态变化时规则将被触发
```

**场景 4：必填字段缺失**
```gherkin
Given 管理员正在创建告警规则
When 未填写规则名称、未选择产品或未配置条件就提交
Then 页面提示缺少必填字段，不发送创建请求
```

---

### 故事 30：查看告警规则列表 [US-PA-030]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看所有告警规则的列表，并按产品和启用状态筛选
**从而**：快速了解各产品的规则配置情况

**【验收标准】**

**场景 1：查看规则列表**
```gherkin
Given 系统中已有多个告警规则
When 管理员进入规则列表页
Then 系统展示所有规则的名称、所属产品、触发类型、启用状态和创建时间
```

**场景 2：按产品筛选规则**
```gherkin
Given 系统中存在多个产品的告警规则
When 管理员选择某个产品进行筛选
Then 系统仅展示属于该产品的规则
```

---

### 故事 31：编辑告警规则 [US-PA-031]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：修改已有告警规则的条件、动作和去重配置
**从而**：根据业务变化调整告警策略

**【验收标准】**

**场景 1：正常编辑规则**
```gherkin
Given 存在一个告警规则
When 管理员修改条件、动作或去重间隔后提交
Then 系统保存修改，页面跳转到规则列表，修改后的信息已更新
```

**场景 2：规则所属产品不可修改**
```gherkin
Given 管理员正在编辑某个告警规则
When 页面加载完成
Then 产品字段为只读状态，无法修改
```

---

### 故事 32：启用/禁用告警规则 [US-PA-032]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：启用或禁用告警规则，而不删除它
**从而**：在不需要触发时暂停规则，需要时快速恢复

**【验收标准】**

**场景 1：禁用规则**
```gherkin
Given 存在一个已启用的告警规则
When 管理员点击"禁用"操作
Then 规则状态变为"已禁用"，设备数据不再触发该规则
```

**场景 2：启用规则**
```gherkin
Given 存在一个已禁用的告警规则
When 管理员点击"启用"操作
Then 规则状态变为"已启用"，设备数据将重新触发该规则
```

---

### 故事 33：删除告警规则 [US-PA-033]

**优先级**: P2

**【用户故事】**
**作为**：Platform Admin
**我希望**：删除不再需要的告警规则
**从而**：保持规则列表整洁

**【验收标准】**

**场景 1：删除规则**
```gherkin
Given 存在一个告警规则
When 管理员点击"删除"并确认操作
Then 系统移除该规则，规则不再出现在列表中
```

**场景 2：删除确认**
```gherkin
Given 管理员点击了某条规则的"删除"
Then 页面弹出确认对话框，展示待删除规则的名称
When 管理员取消删除
Then 规则保持不变
```

---

## 9. 告警记录管理

### 故事 34：查看告警记录 [US-PA-034]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：查看告警触发的历史记录，并按产品、设备、级别和确认状态筛选
**从而**：及时发现和追踪设备异常情况

**【验收标准】**

**场景 1：查看告警记录列表**
```gherkin
Given 系统中已产生多条告警记录
When 管理员进入告警记录列表页
Then 系统展示告警记录的触发时间、规则名称、产品、设备、告警级别和确认状态，按时间倒序排列
```

**场景 2：按产品筛选告警**
```gherkin
Given 系统中存在多个产品的告警记录
When 管理员选择某个产品进行筛选
Then 系统仅展示该产品下的告警记录
```

**场景 3：按未确认状态筛选**
```gherkin
Given 系统中存在已确认和未确认的告警记录
When 管理员筛选未确认的告警
Then 系统仅展示尚未确认的告警记录
```

---

### 故事 35：确认告警 [US-PA-035]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：确认已处理的告警记录
**从而**：标记已知悉的告警，方便团队区分新告警和已处理告警

**【验收标准】**

**场景 1：确认单条告警**
```gherkin
Given 存在一条未确认的告警记录
When 管理员点击"确认"操作
Then 告警状态变为"已确认"，列表中该告警的确认状态已更新
```

**场景 2：已确认告警不可重复确认**
```gherkin
Given 存在一条已确认的告警记录
When 告警列表渲染该行
Then 该行不显示"确认"操作按钮
```

---

## 10. 设备自动注册

### 故事 36：配置产品自动注册 [US-PA-036]

**优先级**: P0

**【用户故事】**
**作为**：Platform Admin
**我希望**：为产品开启或关闭设备自动注册功能
**从而**：允许或禁止该产品下的设备在首次通过 HMAC 认证连接时自动完成注册，无需手动预注册

**【验收标准】**

**场景 1：开启自动注册**
```gherkin
Given 管理员正在编辑某个产品
When 管理员将"设备自动注册"开关设为开启并保存
Then 该产品的自动注册功能生效，持有有效 HMAC 凭据的设备首次连接时将自动创建设备记录
```

**场景 2：关闭自动注册**
```gherkin
Given 某产品已开启自动注册
When 管理员将"设备自动注册"开关设为关闭并保存
Then 新设备不再自动注册，但已自动注册的设备记录保留不受影响
```

**场景 3：新建产品默认关闭**
```gherkin
Given 管理员正在创建新产品
When 产品创建完成
Then "设备自动注册"功能默认为关闭状态
```

---

### 故事 37：查看设备注册来源 [US-PA-037]

**优先级**: P1

**【用户故事】**
**作为**：Platform Admin
**我希望**：在设备列表中区分自动注册和手动注册的设备
**从而**：了解设备的注册方式，便于设备资产管理

**【验收标准】**

**场景 1：设备列表展示注册来源**
```gherkin
Given 系统中存在自动注册和手动注册的设备
When 管理员查看设备列表
Then 每个设备展示其注册来源（自动注册或手动注册）
```

**场景 2：按注册来源筛选**
```gherkin
Given 系统中存在多种注册来源的设备
When 管理员按注册来源进行筛选
Then 系统仅展示匹配注册来源的设备
```

**场景 3：筛选来源无匹配设备**
```gherkin
Given 管理员按注册来源筛选"自动注册"
And 该产品下尚无自动注册的设备
Then 设备列表为空，页面展示"暂无设备"的空状态提示
```
